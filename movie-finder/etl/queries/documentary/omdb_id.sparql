# https://query.wikidata.org/
# documentary -.-> omdb_id
SELECT distinct ?documentary ?omdb_id
with { 
    SELECT distinct ?documentary
    WHERE {
        ?documentary wdt:P31/wdt:P279* wd:Q4164344 .
    }
} as %q0    
WHERE {
    include %q0
    ?documentary wdt:P3302 ?omdb_id .
}
