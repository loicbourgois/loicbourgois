# https://query.wikidata.org/
# documentary -.-> cast_member
SELECT distinct ?documentary ?cast_member
with { 
    SELECT distinct ?documentary
    WHERE {
        ?documentary wdt:P31/wdt:P279* wd:Q4164344 .
    }
} as %q0    
WHERE {
    include %q0
    ?documentary wdt:P161 ?cast_member .
}
