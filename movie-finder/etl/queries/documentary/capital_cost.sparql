# https://query.wikidata.org/
# documentary -.-> capital_cost
SELECT distinct ?documentary ?capital_cost
with { 
    SELECT distinct ?documentary
    WHERE {
        ?documentary wdt:P31/wdt:P279* wd:Q4164344 .
    }
} as %q0    
WHERE {
    include %q0
    ?documentary wdt:P2130 ?capital_cost .
}
