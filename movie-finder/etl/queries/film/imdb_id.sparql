# https://query.wikidata.org/
# film -.-> imdb_id
SELECT distinct ?film ?imdb_id
with { 
    SELECT distinct ?film
    WHERE {
        ?film wdt:P31/wdt:P279* wd:Q11424 .
    }
} as %q0    
WHERE {
    include %q0
    ?film wdt:P345 ?imdb_id .
}
