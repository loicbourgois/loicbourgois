# https://query.wikidata.org/
# anime -.-> main_subject
SELECT distinct ?anime ?main_subject
with { 
    SELECT distinct ?anime
    WHERE {
        ?anime wdt:P31/wdt:P279* wd:Q1107 .
    }
} as %q0    
WHERE {
    include %q0
    ?anime wdt:P921 ?main_subject .
}
