# https://query.wikidata.org/
# anime -.-> omdb_id
SELECT distinct ?anime ?omdb_id
with { 
    SELECT distinct ?anime
    WHERE {
        ?anime wdt:P31/wdt:P279* wd:Q1107 .
    }
} as %q0    
WHERE {
    include %q0
    ?anime wdt:P3302 ?omdb_id .
}
